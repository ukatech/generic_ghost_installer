#include "my-gists/ukagaka/SSPpath.hpp"
int main() {
    if(IsSSPinstalled()) {
        //TODO download and install ghost
    }
    else{
        //TODO download and install SSP
        //get language id
        //show install path dialog
        //install SSP(download zip & extract)
        //chose&install language pack & ghost for starter
        //install ghost
    }
    return 0;
}
